server:
  port: 8081

spring:
  application:
    name: cinema-service

eureka:
  client:
    serviceUrl:
      defaultZone: https://service-registry-n69h.onrender.com/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

