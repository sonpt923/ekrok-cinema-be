server:
  port: 8089

spring:
  application:
    name: schedule-service

eureka:
  client:
    serviceUrl:
      defaultZone: https://service-registry-n69h.onrender.com/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

