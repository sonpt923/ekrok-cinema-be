server:
  port: 8089

spring:
  application:
    name: schedule-service

eureka:
  client:
    serviceUrl:
      defaultZone: https://service-registry-n69h.onrender.com/

management:
  tracing:
    sampling:
      probability: 1.0

